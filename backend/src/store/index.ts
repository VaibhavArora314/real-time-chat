const CONNECTED_USERS:string[] = []; // to be used to make sure that one user is connected once only, i.e. if main chat component page opened in multiple tabs, other tabs will show already opened in other tab

export {
    CONNECTED_USERS
};